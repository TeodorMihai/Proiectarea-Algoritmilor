#include <iostream>
#include <fstream>
 
using namespace std;
 
 
int a[1025][1025],s[1025];
bool b[1025][1025];
 
int min(int a, int b, int c)
{
    if(a<=b && a<=c)
        return a;
    if(b<=a && b<=c)
        return b;
    else
        return c;
}
 
int main()
{
    ifstream fin("note.in");
    ofstream fout("note.out");
    int v,n,m;
    fin>>v>>n;
    for(int i=1; i<=v; i++)
        for(int j=1; j<=n; j++)
        {
            int x;
            fin>>x;
            b[j][x]=true;
        }
    fin>>m;
    for(int i=1; i<=m; i++)
        fin>>s[i];
 
    for(int i=1; i<=n; i++)
        a[i][0]=i;
    for(int i=1; i<=m; i++)
        a[0][i]=i;
 
    for(int i=1; i<=n; i++)
    {
        for(int j=1; j<=m; j++)
        {
            if(b[i][s[j]])
                a[i][j]=a[i-1][j-1];
            else
                a[i][j]=min(a[i-1][j],a[i-1][j-1],a[i][j-1])+1;
        }
    }
    fout<<a[n][m];
    return 0;
}